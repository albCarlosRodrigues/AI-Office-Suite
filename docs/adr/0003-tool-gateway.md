# ADR 0003 — Gateway de ferramentas

Toda mutação passa por registry, policy/risk, aprovação, executor determinístico, idempotência e evidence store. Fallback de backend nunca contorna negação de segurança.
